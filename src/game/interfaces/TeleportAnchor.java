package game.interfaces;

/**
 * Marker interface for magic circle grounds that act as fixed teleportation anchors.
 * Used to locate other anchor tiles on the same map without instanceof checks.
 */
public interface TeleportAnchor {
}
