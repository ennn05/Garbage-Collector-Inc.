package game.interfaces;

/**
 * Marker interface for items that can perform cutting operations on Cuttable objects.
 * Allows the ContractedWorker to detect cutting tools in inventory without
 * using concrete class checks.
 */
public interface CuttingTool {
}
