package game.interfaces;

/**
 * Marker interface for {@link game.flora.FleshySprout} ground tiles.
 * Allows type-safe detection via {@code Location.getGroundAs(FleshySproutGround.class)}
 * without using {@code instanceof}.
 */
public interface FleshySproutGround {
}
