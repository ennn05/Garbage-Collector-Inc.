package game.interfaces;

/**
 * Marker interface for {@link game.flora.FleshyMatureTree} ground tiles.
 * Allows type-safe detection via {@code Location.getGroundAs(FleshyMatureTreeGround.class)}
 * without using {@code instanceof}.
 */
public interface FleshyMatureTreeGround {
}
