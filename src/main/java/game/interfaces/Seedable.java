package game.interfaces;

/**
 * Marker interface for ground tiles that can have fungal growth seeded on them.
 * Only basic floor tiles should implement this; hazard or special grounds should not.
 */
public interface Seedable {
}
